server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/assessment_database
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    web: DEBUG
